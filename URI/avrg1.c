#include<stdio.h>

int main()
{
    double A, B, MEDIA, sum;

         scanf("%lf%lf", &A, &B);
         sum = ((A*3.5) + (B*7.5) );

        MEDIA = sum / 11;

        printf("MEDIA = %.5lf\n", MEDIA);



    return 0;
}
